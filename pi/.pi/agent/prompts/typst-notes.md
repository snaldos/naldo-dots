---
description: Create or improve compile-checked Typst mathematics notes
argument-hint: "<topic/files>"
---

Create or improve Typst notes for:

$@

Load the `typst-math-authoring` skill. Inspect existing imports, `lib.typ`, notation, and compile root before editing. Use Typst-native math with intuition before formalism, explicit assumptions/shapes, examples, counterexamples, and exercises as appropriate. Add abstractions only after real repetition. Compile every changed entry document to a temporary PDF and leave no generated PDF in the source tree.
